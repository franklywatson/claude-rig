A headless service that runs nightly on a schedule, pulls inbound job applicants
from an external ATS API, scores each against a rubric using an LLM, and writes
results back. It must keep running unattended for months.
